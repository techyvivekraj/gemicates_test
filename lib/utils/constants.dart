import 'package:flutter/material.dart';

const String remoteConfigKeyShowDiscountedPrice = 'show_discounted_price';

class ColorsConst {
  static const Color primary = Color(0XFF43B067);
  static const Color textColor = Color(0XFF2B2C2B);
  static const Color white = Color(0XFFFFFFFF);
  static const Color shadow = Color.fromRGBO(0, 0, 0, 0.1);
}
